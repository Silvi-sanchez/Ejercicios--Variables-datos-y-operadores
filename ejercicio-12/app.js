// Ejercicio 12

const numero = prompt("Ingrese un número");

const porcentaje = prompt("Ingrese el porcentaje que desea obtener del mismo");

const result = (numero * porcentaje)/100;

alert(`El resultado de dicho número es de ${result}`);