// Ejercicio 03

const usuarioName = prompt("Ingrese su nombre");

const usuarioLastName = prompt("Ingrese su apellido");

const usuarioAge = prompt("Ingrese su edad");

const usuarioNationality = prompt("Ingrese su nacionalidad");

const usuarioDocument = prompt("Ingrese su documento");

alert(`Nuevo usuario agregado al sistema: 
${usuarioName} 
${usuarioLastName}
${usuarioAge} años 
${usuarioNationality} 
${usuarioDocument}`);